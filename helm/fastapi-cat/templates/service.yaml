apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-cat.fullname" . }}
  labels:
    {{- include "fastapi-cat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-cat.selectorLabels" . | nindent 4 }}
