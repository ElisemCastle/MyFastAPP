apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-cat.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-cat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-cat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
